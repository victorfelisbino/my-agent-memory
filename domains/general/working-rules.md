# General Working Rules

- Confirm done only after code, PR diff, and deployment evidence match.
- Capture root cause before retrying failed steps.
- Validate fixes with the affected user persona, not admin only.
- Add one reusable guardrail after each incident.
